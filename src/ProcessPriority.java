/**
 * This enum represents the priority of a given process
 * It is used in the scheduling queue to determine the order in which processes should be run
 *
 */
public enum ProcessPriority {

    HIGH,MEDIUM,LOW;
}
